# Nearest-City

+ The Nearest City file finds the closest city to a truck, given the x and y coordinates of the cities and the truck.
+ The Nearest Cities file finds the closest k cities to a truck, with quickselect.
+ The Sorted Nearest Cities uses quicksort to find the closest k cities, sorted least to greatest by distance to the truck.
